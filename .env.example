# Example environment (used by docker-compose, optional)
OPENALEX_EMAIL=you@example.com
CROSSREF_MAILTO=you@example.com

# Simulator parameters used by docker-compose
SCENARIO=all        # impact_only,no_control,random,pid,lqr,cpg or 'all'
TRIALS=50           # number of trials to run
